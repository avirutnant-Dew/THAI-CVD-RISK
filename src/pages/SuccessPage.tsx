import { Check, MessageCircle } from 'lucide-react'

export function SuccessPage({ referenceId, requested, onHome }: { referenceId: string; requested: boolean; onHome: () => void }) {
  return <section className="page-wrap text-center"><span className="mx-auto grid size-16 place-items-center rounded-full bg-emerald-100 text-emerald-700"><Check size={30}/></span><h1 className="mt-5 text-3xl font-bold">ขอบคุณที่ทำแบบประเมิน</h1><p className="mx-auto mt-3 max-w-md text-sm leading-6 text-slate-600">{requested ? 'เราได้รับคำขอของคุณแล้ว เจ้าหน้าที่จะติดต่อกลับตามข้อมูลที่ให้ไว้' : 'คุณทำแบบประเมินเรียบร้อยแล้ว โปรดเก็บหมายเลขอ้างอิงนี้ไว้'}</p><div className="mx-auto mt-7 max-w-sm rounded-2xl border border-sage-200 bg-sage-50 p-5"><p className="text-xs text-slate-500">Assessment Reference ID</p><p className="mt-2 break-all font-mono text-xl font-bold tracking-wide">{referenceId}</p></div><div className="mt-8 flex flex-col justify-center gap-3 sm:flex-row"><button type="button" onClick={onHome} className="min-h-12 rounded-xl bg-ink px-6 font-semibold text-white">กลับหน้าหลัก</button>{import.meta.env.VITE_LINE_URL ? <a href={import.meta.env.VITE_LINE_URL} target="_blank" rel="noreferrer" className="inline-flex min-h-12 items-center justify-center gap-2 rounded-xl border border-sage-600 px-6 font-semibold text-sage-700"><MessageCircle size={18}/> LINE โรงพยาบาล</a> : null}</div></section>
}
